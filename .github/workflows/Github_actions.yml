name: Hello this is Nandita(Learning how does github actions work) Change

on: [push]

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Say Hello
        run: echo "👋 Hello, ${{ github.actor }}! This is GitHub Actions."
